<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/tmdb_logo_primary_full"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieDB"
        tools:targetApi="31">
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.favorite.FavoriteActivity"
            android:exported="false" />
        <activity
            android:name=".ui.detail.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <provider
            android:name=".provider.FavoriteProvider"
            android:authorities="com.fortoszone.moviedb"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.fortoszone.moviedb.READ_DATABASE"
            android:writePermission="com.fortoszone.moviedb.WRITE_DATABASE" />
    </application>

</manifest>